#!/usr/bin/env bash

# Usage: get_registration username bridgename bridge_address

curl -sfS \
  -X PUT \
  -H "Authorization: Bearer $ASMUX_SHARED_SECRET" \
  -d "{\"address\": \"$3\"}" \
  http://localhost:29326/_matrix/asmux/appservice/$1/$2 | yq -P
